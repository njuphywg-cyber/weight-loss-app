# Agent 文档 - 一起变轻 App

> 本文档为 AI Agent 提供项目完整信息，用于理解、维护和扩展此应用。

## 📋 项目概述

**项目名称**：一起变轻 - 情侣减肥打卡 App  
**版本**：1.0.0  
**类型**：React + TypeScript Web 应用  
**定位**：情侣专属的减肥打卡与互相鼓励 App，用轻量记录 + 情绪价值引擎，帮助两个人长期坚持并达成健康目标

### 核心价值主张
- **极简打卡**：最少1项即可完成，30秒内结束
- **情绪价值**：AI驱动的共情、鼓励、庆祝、恢复
- **情侣私域**：默认仅双方可见的私域空间
- **不制造焦虑**：趋势为主，隐私可控的数据呈现

---

## 🏗️ 技术架构

### 技术栈
- **前端框架**：React 18.2.0 + TypeScript 5.2.2
- **构建工具**：Vite 5.0.8
- **UI库**：原生 CSS3（渐变、动画、响应式）
- **图表库**：Recharts 2.10.3
- **日期处理**：date-fns 2.30.0
- **数据存储**：localStorage（浏览器本地存储）

### 项目结构
```
weight-loss-app/
├── src/
│   ├── components/          # React组件
│   │   ├── Login.tsx       # 登录/注册组件
│   │   ├── CoupleBinding.tsx  # 情侣绑定组件
│   │   ├── GoalSetup.tsx   # 目标设置组件
│   │   ├── SimpleCheckIn.tsx  # 极简打卡组件（核心）
│   │   ├── CoupleSpace.tsx # 情侣空间组件
│   │   ├── Progress.tsx    # 进展追踪组件
│   │   ├── WeeklyRecap.tsx # 周报组件
│   │   └── Settings.tsx    # 设置组件
│   ├── utils/
│   │   ├── storage.ts      # 数据存储工具（localStorage封装）
│   │   └── aiService.ts    # AI服务（状态识别、情绪反馈等）
│   ├── types.ts            # TypeScript类型定义
│   ├── App.tsx             # 主应用组件（路由管理）
│   ├── main.tsx            # 应用入口
│   └── index.css           # 全局样式
├── index.html              # HTML入口
├── vite.config.ts          # Vite配置
├── tsconfig.json           # TypeScript配置
└── package.json            # 依赖管理
```

---

## 🎯 核心功能模块

### 1. 账号与认证系统
**文件**：`src/components/Login.tsx`  
**功能**：
- 手机号登录/注册（首次自动注册）
- 用户资料管理（昵称、手机号）
- 当前用户状态管理

**数据模型**：`UserProfile`
```typescript
{
  id: string
  name: string
  phone?: string
  startWeight?: number
  targetWeight?: number
  goalPeriod?: number
  recordIntensity?: 'light' | 'standard' | 'advanced'
  stylePreference?: 'cute' | 'calm' | 'funny' | 'serious'
}
```

### 2. 情侣绑定系统
**文件**：`src/components/CoupleBinding.tsx`  
**功能**：
- 创建6位绑定码
- 通过绑定码加入绑定
- 绑定状态管理（isActive）

**数据模型**：`CoupleBinding`
```typescript
{
  id: string
  userId1: string
  userId2: string
  bindCode: string  // 6位大写字母数字
  createdAt: string
  isActive: boolean
}
```

**存储机制**：
- 创建绑定：临时存储在 `temp_bindings`（localStorage）
- 完成绑定：保存到 `weight-loss-couples`（localStorage）
- 解绑：设置 `isActive = false`

### 3. 目标设置系统
**文件**：`src/components/GoalSetup.tsx`  
**功能**：
- 三步设置流程：目标 → 记录强度 → 文案风格
- 体重目标设置（起始/目标/周期）
- 记录强度选择（轻量/标准/进阶）
- 文案风格偏好（可爱/克制/搞笑/认真）

**数据模型**：`Goal`, `CoupleGoal`

### 4. 极简打卡系统（核心功能）
**文件**：`src/components/SimpleCheckIn.tsx`  
**功能**：
- **日期选择**：今天/昨天/前天（可补卡过去2天）
- **打卡字段**：
  - 运动：走路/有氧/力量/拉伸（可多选）
  - 饮食：正常/控制不错/稍放纵/暴食
  - 喝水：达标/未达标
  - 睡眠：好/一般/差
  - 心情：🙂 😐 🙁 😣 🤩
  - 备注：一句话（可选）
  - 体重：kg（可选）
- **极简规则**：至少1项即可完成打卡
- **AI反馈**：提交后自动生成情绪反馈卡
- **伴侣互动**：一键发送鼓励（夸夸/抱抱/小任务）

**关键逻辑**：
```typescript
// 日期切换时自动加载数据
useEffect(() => {
  const checkIn = storage.getCheckInByDate(userId, selectedDate)
  // 更新表单数据
}, [selectedDate, userId])

// 补卡限制：只能补过去2天
if (daysDiff > 2) {
  alert('只能补卡过去2天内的记录')
  return
}
```

**数据模型**：`CheckInEntry`
```typescript
{
  id: string
  userId: string
  date: string  // yyyy-MM-dd
  exercises?: ExerciseType[]
  diet?: DietType
  water?: boolean
  sleep?: SleepQuality
  mood?: MoodType
  note?: string
  weight?: number
  aiState?: AIStateClassification
  feedbackCard?: EmpathyFeedback
}
```

### 5. AI情绪反馈引擎
**文件**：`src/utils/aiService.ts`  
**功能**：

#### 节点1：状态识别/分类
- **输入**：打卡结构化数据 + 备注 + 历史记录
- **输出**：`AIStateClassification`
  - `effortLevel`: 'high' | 'mid' | 'low'
  - `moodState`: 'positive' | 'neutral' | 'low' | 'anxious' | 'irritable'
  - `riskFlag`: 'binge' | 'self_blame' | 'overtraining_suspect'
  - `contextHint`: 'busy' | 'period' | 'social_event' | 'travel' | 'sick'
  - `recommendedTone`: 'cute' | 'calm' | 'funny' | 'serious'

**策略**：规则优先 + 关键词匹配

#### 节点2：情绪反馈生成
- **输入**：状态标签 + 完成项 + 用户偏好
- **输出**：`EmpathyFeedback`
  - `title`: <=12字
  - `empathyLine`: <=24字（共情）
  - `achievementLine`: <=24字（成就）
  - `microAction`: <=14字（小动作）
  - `styleTag`: 文案风格

**策略**：80%模板库 + 20%可扩展LLM（当前全用模板）

#### 节点3：伴侣互动推荐
- **输入**：当前用户状态 + 伴侣状态 + 历史互动
- **输出**：`CheerType` ('praise' | 'hug' | 'micro_task')

**规则**：
- mood low/anxious → hug
- effort high → praise
- effort low → micro_task

#### 节点4：断卡风险预测（已实现基础版）
- **输入**：连续打卡天数、完成度、心情分布
- **输出**：风险评分、干预类型

#### 节点5：周报总结
- **输入**：过去7天打卡数据
- **输出**：亮点、进步、下周小目标、情侣互动时刻

### 6. 情侣空间
**文件**：`src/components/CoupleSpace.tsx`  
**功能**：
- 双方今日打卡状态展示（遵循隐私设置）
- 情侣能量条（综合完成度，不排名）
- 共同目标进度条 + 里程碑（10%/50%/100%）
- 鼓励墙（互动卡时间流）

**隐私控制**：
- 根据 `PrivacySettings` 控制显示内容
- 字段级共享：体重/围度/照片/心情/备注

### 7. 进展追踪
**文件**：`src/components/Progress.tsx`  
**功能**：
- **统计卡片**：连续打卡、总打卡次数、运动天数、喝水达标
- **体重趋势图**：7/30/90天折线图（Recharts）
- **习惯热力图**：打卡/运动可视化
- **里程碑时间轴**：7/14/30连续打卡庆祝

**连续打卡计算**：
```typescript
// 去重并按日期排序（最新的在前）
const uniqueDates = [...new Set(checkIns.map(c => c.date))].sort().reverse()
let streak = 0
let expectedDate = today
for (const date of uniqueDates) {
  if (date === expectedDate) {
    streak++
    expectedDate = getPreviousDate(date)
  } else {
    break
  }
}
```

### 8. 周报系统
**文件**：`src/components/WeeklyRecap.tsx`  
**功能**：
- 自动生成本周周报（周一至周日）
- 亮点总结、进步回顾、下周小目标
- 情侣互动时刻记录

### 9. 设置与隐私
**文件**：`src/components/Settings.tsx`  
**功能**：
- **提醒设置**：
  - 每日打卡提醒（时间可配置）
  - 伴侣打卡提醒
  - 周报提醒
- **隐私设置**：
  - 字段级共享控制（体重/围度/照片/心情/备注）
- **情侣绑定**：查看绑定对象、解绑功能
- **退出登录**

---

## 💾 数据存储系统

### 存储工具
**文件**：`src/utils/storage.ts`  
**存储方式**：localStorage（浏览器本地存储）

### 存储键名（STORAGE_KEYS）
```typescript
{
  WEIGHT_ENTRIES: 'weight-loss-entries',
  USER_PROFILES: 'weight-loss-profiles',
  CHECK_IN_ENTRIES: 'weight-loss-checkins',
  COUPLE_BINDINGS: 'weight-loss-couples',
  GOALS: 'weight-loss-goals',
  COUPLE_GOALS: 'weight-loss-couple-goals',
  CHEER_CARDS: 'weight-loss-cheers',
  REMINDER_SETTINGS: 'weight-loss-reminders',
  PRIVACY_SETTINGS: 'weight-loss-privacy',
  WEEKLY_RECAPS: 'weight-loss-recaps',
  MILESTONES: 'weight-loss-milestones',
  CURRENT_USER_ID: 'weight-loss-current-user',
}
```

### 核心方法
- `getCurrentUserId()` / `setCurrentUserId()`
- `getUserProfile()` / `saveUserProfile()`
- `getCheckInEntries()` / `saveCheckInEntry()`
- `getCoupleBindingByUserId()` / `saveCoupleBinding()`
- `getCheerCards()` / `saveCheerCard()`
- `checkAndCreateMilestones()` - 里程碑自动检测

### 里程碑检测逻辑
**触发时机**：每次打卡后自动检测

**检测类型**：
1. **连续打卡里程碑**：
   - streak_7: 连续打卡7天
   - streak_14: 连续打卡14天
   - streak_30: 连续打卡30天

2. **目标进度里程碑**：
   - goal_10: 目标进度10%
   - goal_50: 目标进度50%
   - goal_100: 目标进度100%

**计算方式**：
```typescript
// 连续打卡：从今天向前检查连续性
// 目标进度：(当前变化 / 总变化) * 100
const progressPercent = (currentChange / totalChange) * 100
```

---

## 🎨 UI/UX 设计原则

### 设计理念
1. **不制造焦虑**：趋势为主，隐私可控
2. **情绪价值优先**：共情、鼓励、庆祝、恢复
3. **极简体验**：30秒完成打卡
4. **情侣私域**：默认仅双方可见

### 视觉风格
- **主色调**：渐变紫色（#667eea → #764ba2）
- **卡片设计**：圆角15px，白色背景，阴影
- **按钮**：渐变背景，hover效果，圆角
- **响应式**：支持移动端和桌面端

### 关键交互
- **日期切换**：点击日期按钮，自动加载对应数据
- **表单提交**：极简验证（至少1项），提交后显示反馈卡
- **反馈卡**：共情 → 成就 → 小动作，可发送给伴侣
- **导航**：底部固定导航栏（今日/情侣空间/进展/周报/我的）

---

## 🔧 开发指南

### 环境要求
- Node.js >= 16
- npm >= 7

### 安装与启动
```bash
# 安装依赖
npm install

# 开发模式
npm run dev
# 访问 http://localhost:5173 或 http://127.0.0.1:5173

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

### 开发注意事项

#### 1. 状态管理
- 使用 React Hooks（useState, useEffect）
- 数据存储在 localStorage，通过 `storage` 工具访问
- 组件间通过 props 传递数据

#### 2. 日期处理
- 统一使用 `date-fns` 库
- 日期格式：`yyyy-MM-dd`（存储），`yyyy年MM月dd日`（显示）
- 时区：使用本地时区

#### 3. 类型安全
- 所有组件使用 TypeScript
- 类型定义在 `src/types.ts`
- 严格模式开启（strict: true）

#### 4. 样式管理
- 每个组件有独立的 CSS 文件
- 全局样式在 `src/index.css`
- 使用 CSS 变量（可扩展）

#### 5. 错误处理
- 表单验证：alert 提示（可改进为 Toast）
- 数据加载：检查 localStorage 是否存在
- 空值检查：使用可选链操作符（?.）

---

## 🐛 已知问题与限制

### 当前限制
1. **数据存储**：
   - 使用 localStorage，清除浏览器数据会丢失
   - 不支持多设备同步
   - 无数据备份/恢复功能

2. **功能限制**：
   - 二维码绑定未实现（仅6位口令）
   - LLM生成反馈未实现（全用模板）
   - 推送通知未实现
   - 照片上传未实现（仅支持URL）

3. **性能**：
   - 大量数据时可能影响性能
   - 无虚拟滚动
   - 无数据分页

### 已修复的问题
1. ✅ 日期切换后数据不更新
2. ✅ 连续打卡天数计算错误
3. ✅ 目标进度里程碑计算错误
4. ✅ 空值检查缺失
5. ✅ 隐私设置加载位置错误

### 待优化项
- [ ] 添加错误边界（Error Boundary）
- [ ] 添加加载状态指示器
- [ ] 优化性能（useMemo, useCallback）
- [ ] 添加单元测试
- [ ] 添加数据导出功能
- [ ] 改进错误提示（Toast组件）

---

## 📊 数据流

### 打卡流程
```
用户选择日期 → 加载/清空表单数据
  ↓
用户填写打卡信息（至少1项）
  ↓
提交 → AI状态识别 → 生成反馈卡
  ↓
保存打卡记录 → 检测里程碑 → 显示反馈卡
  ↓
用户可选择发送鼓励给伴侣
```

### 情侣绑定流程
```
用户A创建绑定码 → 临时存储（temp_bindings）
  ↓
用户B输入绑定码 → 查找临时绑定
  ↓
完成绑定 → 保存到正式存储 → 移除临时绑定
  ↓
双方可访问情侣空间
```

### 里程碑检测流程
```
打卡保存后 → checkAndCreateMilestones()
  ↓
计算连续打卡天数 → 检查里程碑条件
  ↓
计算目标进度 → 检查里程碑条件
  ↓
创建新里程碑 → 保存 → 显示庆祝
```

---

## 🔐 安全与隐私

### 数据安全
- 所有数据存储在浏览器本地（localStorage）
- 无服务器传输，无数据泄露风险
- 清除浏览器数据 = 清除所有数据

### 隐私控制
- 字段级共享控制（体重/围度/照片/心情/备注）
- 默认保守设置（仅心情共享）
- 可随时调整隐私设置
- 解绑后对方不可见历史内容

### AI内容安全
- 不提供医疗诊断
- 不提供极端建议
- 不羞辱、不恐吓
- 温柔恢复路径

---

## 🚀 扩展建议

### 短期（P1）
1. **二维码绑定**：使用二维码生成库
2. **数据导出**：导出为 CSV/JSON
3. **推送通知**：浏览器通知 API
4. **照片上传**：图片压缩和存储

### 中期（P2）
1. **后端集成**：数据云端同步
2. **LLM集成**：调用 API 生成个性化反馈
3. **数据分析**：更详细的统计和分析
4. **社交功能**：分享成就到外部平台

### 长期（P3）
1. **移动端App**：React Native
2. **多语言支持**：国际化
3. **AI教练**：个性化建议和计划
4. **社区功能**：用户社区和分享

---

## 📝 代码规范

### 命名规范
- **组件**：PascalCase（如 `SimpleCheckIn.tsx`）
- **文件**：PascalCase（组件）或 camelCase（工具）
- **变量/函数**：camelCase
- **常量**：UPPER_SNAKE_CASE
- **类型/接口**：PascalCase

### 代码风格
- 使用 TypeScript 严格模式
- 函数组件优先
- Hooks 使用规范
- 注释：关键逻辑添加注释

### Git 提交规范
- feat: 新功能
- fix: 修复bug
- docs: 文档更新
- style: 代码格式
- refactor: 重构
- test: 测试
- chore: 构建/工具

---

## 🧪 测试建议

### 功能测试清单
- [ ] 登录/注册流程
- [ ] 情侣绑定流程（创建/加入）
- [ ] 目标设置流程
- [ ] 打卡功能（今天/昨天/前天）
- [ ] 补卡限制（超过2天）
- [ ] AI反馈生成
- [ ] 发送鼓励给伴侣
- [ ] 情侣空间显示
- [ ] 隐私设置生效
- [ ] 里程碑检测和庆祝
- [ ] 周报生成
- [ ] 数据持久化

### 边界测试
- [ ] 空数据状态
- [ ] 大量数据性能
- [ ] 日期边界（跨月/跨年）
- [ ] 绑定码重复
- [ ] 解绑后数据访问

---

## 📚 参考文档

- [React 官方文档](https://react.dev)
- [TypeScript 官方文档](https://www.typescriptlang.org)
- [Vite 官方文档](https://vitejs.dev)
- [Recharts 文档](https://recharts.org)
- [date-fns 文档](https://date-fns.org)

---

## 🎯 项目目标

### 北极星指标
**情侣双人周有效打卡率**：两人每周各 ≥4 天打卡的比例

### 过程指标
- 激活：绑定成功率、首次打卡完成率（T+1）
- 留存：D1 / D7 / D30
- 参与：人均周打卡次数、连续打卡天数中位数
- 情绪价值：鼓励卡发送率、收到鼓励后的次日回访 uplift
- 目标：里程碑达成率（7/14/30 连续、目标进度 10/50/100%）

---

**最后更新**：2026-02-02  
**维护者**：AI Agent  
**版本**：1.0.0

---

> 💡 **提示**：本文档应随项目更新而更新，确保信息准确性和完整性。
